fastapi==0.115.0
uvicorn[standard]==0.30.6
SQLAlchemy==2.0.35
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
openai==1.51.0
# openai 1.51.0's internal http client setup still passes `proxies=`, which
# httpx>=0.28 removed (TypeError: Client.__init__() got an unexpected
# keyword argument 'proxies'). Pin httpx below that until openai is bumped.
httpx==0.27.2
